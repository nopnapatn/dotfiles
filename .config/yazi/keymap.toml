# Yazi keybindings.
# Customize on top of defaults via prepend_keymap / append_keymap (see
# https://yazi-rs.github.io/docs/configuration/overview#configuration-mixing).
# Full keymap reference: https://yazi-rs.github.io/docs/configuration/keymap

"$schema" = "https://yazi-rs.github.io/schemas/keymap.json"

[mgr]
prepend_keymap = []
append_keymap = []
